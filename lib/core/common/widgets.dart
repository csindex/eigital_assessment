import 'package:flutter/material.dart';

Widget vSpacer(double? h) => SizedBox(
      height: h ?? 0.0,
      width: 0.0,
    );
Widget hSpacer(double? w) => SizedBox(
      width: w ?? 0.0,
      height: 0.0,
    );
